package com.dbwb.platform.website.entity;

/** BR-ORD-001: each website supports either Display Only or WhatsApp Ordering mode. */
public enum OrderingMode {
    DISPLAY_ONLY,
    WHATSAPP_ORDERING
}
